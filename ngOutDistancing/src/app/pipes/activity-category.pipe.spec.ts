import { ActivityCategoryPipe } from './activity-category.pipe';

describe('ActivityCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivityCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
